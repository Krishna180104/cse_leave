const Approvals = () => {
    return (
        <div>
            <h1 className="text-2xl font-bold">Admin Approvals Page</h1>
            <p>Admins can approve or reject leave applications here.</p>
        </div>
    );
};

export default Approvals; // ✅ Ensure default export
